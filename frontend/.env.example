# For local dev this can stay unset (Vite proxies /api to localhost:5000).
# For production, set this to your deployed backend URL, e.g.:
# VITE_API_URL=https://task-manager-backend.onrender.com/api
VITE_API_URL=
